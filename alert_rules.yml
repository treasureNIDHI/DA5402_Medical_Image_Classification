groups:
  - name: basic-alerts
    rules:
      - alert: API_Down
        expr: up{job="medical-api"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Medical API is down"
          description: "The FastAPI service is not responding"